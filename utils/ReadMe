实验目的：将pdf转换为数据库
步骤：
一、pdf转换成html:pdf2htmlEX
二、html解析:bs4.BeautifulSoup
三、将html分析结果存入标准的数据库
具体步骤：
    1、检查文件名，并解析文件名：
        通过文件名获取市场、证券代码和报告日期信息
        文件名命名规则举例：’sz'+'000001'+'20171231'
        return:
            self.market = 'sz'
            self.code = '000001'
            self.accper = 'date(2017,12,31)
    2、通过bs解析html
        将解析后的内容存储到临时表中


    1、分析html,将html进行分类分解
     html解析成单个行元素，将文件中的每一行或每一个单元格作为一个元素；
    2、建立对标准类别的拆分模式，根据索引开始位置到下一个索引开始位置进行拆分类
    对元素按照标准索引进行分类；（判断标准索引是否已经涵盖了文中所有的索引，没有的话，考虑向标准索引中增加元素）
    标准索引名；标准索引编码；索引开始位置
    3、为每一个类别建立对应的数据库表
    索引类别；索引编码；表名；表编码；表的特征；（表编码是唯一的）
    4、建立标准的数据库系统，取得想要的数据；
    将每个类别中的元素解析成标准的表格。
    表名；表编码；字段编码；字段类型；字段别名；字段长度；（表编码+字段编码）是惟一的





